{
  "name": "mothx-desktop",
  "version": "1.3.100",
  "private": true,
  "description": "MothX desktop client (Electron ACP client + standalone renderer)",
  "author": {
    "name": "MothX Team",
    "email": "admin@pkold.com"
  },
  "homepage": "https://mothx.net",
  "type": "module",
  "main": "dist/main.cjs",
  "mothxRuntime": {
    "version": "1.3.100"
  },
  "scripts": {
    "vendor": "node scripts/build-runtime.cjs",
    "build:runtime": "node scripts/build-runtime.cjs",
    "build": "tsx scripts/build.ts",
    "dev": "tsx scripts/dev.ts",
    "typecheck": "tsc --noEmit",
    "e2e": "node scripts/e2e-smoke.mjs",
    "verify:phase1": "node scripts/phase1-verify.mjs",
    "verify:phase3": "node scripts/phase3-verify.mjs",
    "test": "node --test scripts/resolve-version.test.cjs scripts/e2e-smoke.test.mjs scripts/dev.test.mjs && tsx --test main/*.test.ts renderer/src/*.test.ts",
    "start": "npm run version:set && npm run ensure:electron && npm run build:runtime && npm run build && electron .",
    "ensure:electron": "node scripts/ensure-electron.cjs",
    "dist": "npm run version:set && npm run ensure:electron && npm run build:runtime && npm run build && electron-builder --config electron-builder.yml",
    "dist:dir": "npm run version:set && npm run ensure:electron && npm run build:runtime && npm run build && electron-builder --dir --config electron-builder.yml",
    "dist:dev:mac": "npm run version:set && npm run ensure:electron && npm run build:runtime -- --platform darwin && npm run build && electron-builder --mac --publish never --config electron-builder.yml --arch $(node -p process.arch)",
    "dist:dev:win": "npm run version:set && npm run ensure:electron && npm run build:runtime -- --platform win32 && npm run build && electron-builder --win --publish never --config electron-builder.yml",
    "dist:dev:linux": "npm run version:set && npm run ensure:electron && npm run build:runtime -- --platform linux && npm run build && electron-builder --linux --publish never --config electron-builder.yml",
    "dist:mac": "npm run version:set && npm run ensure:electron && npm run build:runtime -- --platform darwin && npm run build && electron-builder --mac --config electron-builder.yml --arch $(node -p process.arch)",
    "dist:win": "npm run version:set && npm run ensure:electron && npm run build:runtime -- --platform win32 && npm run build && electron-builder --win --config electron-builder.yml",
    "dist:linux": "npm run version:set && npm run ensure:electron && npm run build:runtime -- --platform linux && npm run build && electron-builder --linux --config electron-builder.yml",
    "version:set": "node scripts/set-version.cjs",
    "version:check": "tsx scripts/check-release-version.ts"
  },
  "devDependencies": {
    "electron": "^39.2.7",
    "electron-builder": "26.0.12",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
